# Here we will explore the "UScensus20xx" packages. There is a good overview
# of these packages here: http://www.jstatsoft.org/v37/i06/paper

library(UScensus2010)
# install shape files using the following: install.cdp("osx")
library(UScensus2010cdp)